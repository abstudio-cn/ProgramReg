#pragma once
// targetver.h: 指定要使用的 Windows 平台版本（参考 WindowsProject1 模板）
//
// 包括 SDKDDKVer.h 将定义可用的最高版本的 Windows 平台。
// 如果要为以前的 Windows 平台生成应用程序，请包含 WinSDKVer.h，并将
// _WIN32_WINNT 宏设置为要支持的平台，然后再包含 SDKDDKVer.h。

#include <SDKDDKVer.h>
